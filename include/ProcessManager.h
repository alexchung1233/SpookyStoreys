#ifndef PROCESS_MANAGER_H // include guard
#define PROCESS_MANAGER_H
#include "GameView.h"

class ProcessManager
{
  public:
    bool init();
    bool update();
    bool shutdown();

  };

#endif /* MY_CLASS_H */
